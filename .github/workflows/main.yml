name: Workflow Sync
on:
  push: 
    branches:
      - master
    workflow_dispatch:
jobs:
    your_npm_job_name:
        steps:
          runs: 
            using: 'node16'
            main: "generate.js"

